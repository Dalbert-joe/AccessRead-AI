import './globals.css';
export const metadata={title:'AccessRead AI',description:'Transform PDFs and webpages into accessible content.'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
